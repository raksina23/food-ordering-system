"scripts": {
    "start": "node src/backend/server.js",
    "install:backend": "npm install --prefix src/backend",
    "build": "npm install --prefix src/frontend && npm run build --prefix src/frontend && cp -r src/frontend/dist src/backend/public"
  }